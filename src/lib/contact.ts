/**
 * Inbox for Web3Forms and mailto fallback.
 * Optional: set `VITE_WEB3FORMS_ACCESS_KEY` in `.env` for in-browser POST to web3forms.com (no mailto).
 */
export const CONTACT_INBOX_EMAIL = 'info@missout.in'

/** Display + `tel:` href for direct outreach on the contact section. */
export const CONTACT_PHONE_DISPLAY = '+91 97171 30609'
export const CONTACT_PHONE_HREF = 'tel:+919717130609'
